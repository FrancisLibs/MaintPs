<nav class="navbar navbar-expand-lg navbar-light bg-light mt-4">
    <div class="container justify-content-between">
        <div class="justify-content-start">
            <a class="navbar-brand" href="{{ path('home') }}><img class="logo" src="/img/logo2.jpg" alt="Charcuterie Pierre Schmidt"></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        <div>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 mr-5">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="#" class="btn btn-success navbarlink">Administration</a>
                        <a href="{{ path('user_create') }}" class="btn btn-primary navbarlink">Inscription</a>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                        <a class="nav-link" aria-current="page" href="{{ path('order_new') }}">Commandes</a>
                    {% endif %}
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}" class="pull-right btn btn-danger ml-2 navbarlink">Déconnexion</a>
                    {% else %}
                        <a class="nav-link btn-sm" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
                    {% endif %}
                    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a href="{{ path('user_create') }}" class="btn btn-primary ml-2 navbarlink">Inscription</a>
                    {% endif %}

                </ul>
            </div>
        </div>
    </div>
</nav>